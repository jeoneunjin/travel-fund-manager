{
  "enabledPlugins": {
    "ponytail@ponytail": true
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // empty' | grep -qiE 'rm +(-[a-z]*r[a-z]*f|-[a-z]*f[a-z]*r)|git +push[^|]*(--force|-f\\b)|prisma +migrate +reset|drop +table' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"위험한 명령 감지: rm -rf / git push --force / prisma migrate reset / DROP TABLE\"}}' || true"
          }
        ]
      }
    ]
  }
}
